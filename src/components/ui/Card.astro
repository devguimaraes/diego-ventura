---
import { Card as ShadcnCard } from './card';

export interface Props {
  class?: string;
}

const { class: extraClass = '' } = Astro.props;
---

<ShadcnCard className={extraClass}>
  <slot />
</ShadcnCard>
